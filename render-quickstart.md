# Skills Arena - Render.com 快速部署指南

## 🎯 5 分钟部署到公网

---

## 第一步：准备 GitHub 仓库（2 分钟）

### 1.1 创建 GitHub 仓库

1. 访问 https://github.com/new
2. 仓库名：`skills-arena`
3. 设为 Public（Render 免费版需要公开仓库）
4. 点击 "Create repository"

### 1.2 推送代码到 GitHub

打开终端，在 `skills-arena/` 目录下执行：

```bash
# 初始化 Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "Skills Arena v1.0 - Ready for Render deployment"

# 连接 GitHub（替换为你的用户名）
git remote add origin https://github.com/YOUR_USERNAME/skills-arena.git

# 推送代码
git push -u origin main
```

**注意**：如果遇到错误，尝试：
```bash
git branch -M main
```

---

## 第二步：在 Render.com 创建应用（3 分钟）

### 2.1 注册 Render

1. 访问 https://render.com
2. 点击 "Sign Up"
3. 选择 "Sign up with GitHub"
4. 授权 Render 访问你的 GitHub

### 2.2 创建 Web Service

1. 登录后，点击 "New +"
2. 选择 "Web Service"

### 2.3 配置应用

**连接仓库**：
- 找到你的 `skills-arena` 仓库
- 点击 "Connect"

**配置信息**：
- **Name**: `skills-arena`
- **Region**: 选择离你最近的区域
- **Runtime**: `Python 3`
- **Branch**: `main`

**构建命令**：
```
pip install -r requirements.txt
```

**启动命令**：
```
gunicorn scripts.web_server:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120
```

**实例类型**：
- **Free**（免费版）

### 2.4 点击部署

点击 "Create Web Service"

**等待 3-5 分钟**，Render 会：
1. 克隆你的代码
2. 安装依赖
3. 启动服务器

部署成功后，你会看到：
- ✅ Live Site: `https://skills-arena.onrender.com`

---

## 第三步：初始化数据（1 分钟）

### 3.1 访问初始化端点

在浏览器中访问：

```
https://skills-arena.onrender.com/api/init
```

如果看到：
```json
{
  "status": "success",
  "message": "Demo data initialized successfully"
}
```

**初始化成功！** 🎉

### 3.2 访问应用

打开浏览器访问：

```
https://skills-arena.onrender.com
```

你会看到 Skills Arena 的完整界面！

---

## 🔧 高级配置（可选）

### 环境变量配置

在 Render 的 Web Service 设置中，添加以下环境变量：

| Key | Value |
|-----|-------|
| `FLASK_ENV` | `production` |
| `SECRET_KEY` | `your-secret-key-here` |

### 自定义域名（需要付费）

1. 在 Render 的 Web Service 设置中
2. 点击 "Domains"
3. 添加你的域名
4. 配置 DNS 记录

---

## ⚠️ 常见问题

### 1. 部署失败：模块找不到

**原因**：`requirements.txt` 缺少依赖

**解决**：检查 `scripts/web_server.py` 中的 import，确保所有依赖都在 `requirements.txt` 中

### 2. 502 Bad Gateway

**原因**：服务器启动失败

**解决**：
1. 检查 Render 日志
2. 确认 `Procfile` 中的启动命令正确
3. 确认 `$PORT` 环境变量使用正确

### 3. 数据初始化失败

**原因**：权限问题或路径错误

**解决**：
1. 检查 Render 日志
2. 确认 `data/` 目录结构正确
3. 尝试手动初始化

### 4. 应用休眠

**原因**：免费版 15 分钟无活动会休眠

**解决**：
- 这是正常的，首次访问会自动唤醒
- 如需保持活跃，使用付费版

---

## 📊 监控和日志

### 查看日志

在 Render 的 Web Service 页面：
- 点击 "Logs"
- 实时查看服务器日志

### 监控资源使用

在 Render 的 Web Service 页面：
- 点击 "Metrics"
- 查看 CPU、内存使用情况

---

## 🚀 下一步

### 优化建议

1. **添加数据库**：使用 Render 的 PostgreSQL（免费）
2. **缓存优化**：添加 Redis 缓存
3. **CDN 加速**：使用 CloudFlare CDN
4. **自动备份**：设置定时任务备份数据

### 扩展功能

1. **用户认证**：添加登录系统
2. **支付集成**：添加 Stripe 支付
3. **邮件通知**：添加邮件发送功能
4. **数据分析**：添加数据统计面板

---

## 📞 需要帮助？

- Render 文档：https://render.com/docs
- Flask 文档：https://flask.palletsprojects.com/
- GitHub Issues：在你的仓库创建 Issue

---

## 🎉 完成！

你的 Skills Arena 现在已经部署到公网了！

访问：`https://skills-arena.onrender.com`

**分享给朋友，一起体验！** 🚀
